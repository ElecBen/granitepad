[project]
name = "granitepad"
version = "0.3.0"
description = "Dibuja barras de progreso y avisos de avance en texto"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["progreso", "barra", "terminal", "avance", "cli"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
